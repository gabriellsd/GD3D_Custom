import { initShell } from '../layout/shell.js';
import { initBudgetForm } from '../budget/form.js';

initShell({
  page: 'contato',
  title: 'Projeto à Medida — GD3D Creative',
});

initBudgetForm();
